import styled from 'styled-components'

const StyledIconBoxIcon = styled.svg`
	width: 24px;
	height: 24px;
	cursor: pointer;
	transition: transform 0.2s;

	&:hover {
		transform: scale(1.2);
	}
`

export { StyledIconBoxIcon }
